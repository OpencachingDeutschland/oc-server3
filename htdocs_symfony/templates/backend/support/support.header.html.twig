{# Kopfzeile für alle Support-Unterseiten #}

<div class="container">
    <section class="container col-md-6">
        <form>
            <div class="form-row">
                <div class="form-group col-md-6">
                    <input type="text" class="form-control" placeholder="%%%">
                </div>
                <div class="form-group col-md-1">
                    <a class="btn btn-primary" href="{{ path('backend_support_search') }}"><i class="fa fa-search"></i></a>
                </div>
                <div class="form-group col-md-1">
                    <a class="btn btn-primary" href="{{ path('backend_support_search') }}">=1</i></a>
                </div>
            </div>
        </form>

    </section>

    <section class="container col-md-3">
        <a href="{{ path('backend_support_reported_caches') }}">{{ 'Reported caches' | trans }}</a>
    </section>

    <section class="container col-md-3">
        <a href="{{ path('backend_support_db_queries') }}">{{ 'Database queries' | trans }}</a>
    </section>

</div>

<hr>
