<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            {{ encore_entry_link_tags('backend') }}
        {% endblock %}
    </head>
    <body>
    <div class="container">
        {% include '/index/navigation.html.twig' %}
    </div>

        {% block body %}

            <div>
               <a href="http://try.docker.team-opencaching.de/login">Login</a>
            </div>

            {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('backend_index_index') }}">BACKEND</a>
            {% endif %}

        {% endblock %}
        {% block javascripts %}
            {{ encore_entry_script_tags('backend') }}
        {% endblock %}
    </body>
</html>
